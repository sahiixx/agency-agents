"""WhatsApp web automation placeholder."""

from __future__ import annotations


class WhatsAppWeb:
    """Queue outgoing WhatsApp messages."""

    def send(self, recipient: str, message: str) -> str:
        return f"Queued WhatsApp message to {recipient}: {message}"
