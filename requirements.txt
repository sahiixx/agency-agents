# The Agency — Python dependencies
# Install: pip install -r requirements.txt

anthropic>=0.84.0
langchain>=1.2.12
langchain-anthropic>=1.3.4
langchain-core>=1.2.18

# deepagents SDK — install from local path:
# pip install -e deepagents/libs/deepagents
