name: CI — Claude-Powered Test Suite

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  structural-tests:
    name: Structural Tests (no API key)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deepagents SDK
        run: |
          cd deepagents/libs/deepagents
          pip install -e . --quiet --break-system-packages
          pip install langchain-anthropic anthropic --quiet --break-system-packages

      - name: Run structural tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/deepagents/libs/deepagents
          python3 tests/agent_tests.py -v 2>&1

      - name: Run security audit swarm tests
        run: |
          python3 tests/test_security_audit_swarm.py -v 2>&1

      - name: Run real estate swarm tests
        run: |
          python3 tests/test_real_estate_swarm.py -v 2>&1

  lint-agents:
    name: Lint Agent Files
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get changed agent files
        id: changed
        run: |
          FILES=$(git diff --name-only --diff-filter=ACMR origin/${{ github.base_ref }}...HEAD -- \
            'design/**/*.md' 'engineering/**/*.md' 'marketing/**/*.md' 'sales/**/*.md' \
            'specialized/**/*.md' 'testing/**/*.md' 'support/**/*.md' \
            'project-management/**/*.md' 'product/**/*.md')
          {
            echo "files<<ENDOFLIST"
            echo "$FILES"
            echo "ENDOFLIST"
          } >> "$GITHUB_OUTPUT"

      - name: Lint changed agents
        if: steps.changed.outputs.files != ''
        env:
          CHANGED_FILES: ${{ steps.changed.outputs.files }}
        run: |
          chmod +x scripts/lint-agents.sh
          ./scripts/lint-agents.sh $CHANGED_FILES

  live-llm-tests:
    name: Live LLM Tests (Claude API)
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd deepagents/libs/deepagents
          pip install -e . --quiet --break-system-packages
          pip install langchain-anthropic anthropic --quiet --break-system-packages

      - name: Run live LLM tests
        if: env.ANTHROPIC_API_KEY != ''
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/deepagents/libs/deepagents
          python3 tests/agent_tests.py -v 2>&1

  jarvis-quality:
    name: JARVIS Quality Gates
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install quality tools
        run: |
          pip install --quiet ruff mypy pytest bandit
          pip install --quiet -e .

      - name: Lint JARVIS
        run: ruff check jarvis tests/test_*.py

      - name: Type check JARVIS
        run: mypy jarvis --ignore-missing-imports

      - name: Run JARVIS tests
        run: pytest -q tests/test_*.py

      - name: Security scan
        run: bandit -q -r jarvis

      - name: Build Docker image
        run: docker build -t jarvis-ci:latest .
